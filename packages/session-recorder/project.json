{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "session-recorder",
  "projectType": "library",
  "sourceRoot": "packages/session-recorder/src",
  "tags": ["scope:recorder", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "rslib build", "cwd": "packages/session-recorder" },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/session-recorder"
      },
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests",
        "cwd": "packages/session-recorder"
      },
      "cache": true
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint src", "cwd": "packages/session-recorder" },
      "cache": true
    }
  }
}
